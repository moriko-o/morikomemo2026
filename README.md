# morikomemo

This repository now boots as a static local preview under Rails 8.
OAuth, Twitter posting, and database-backed memo features are disabled for now.

## Run locally

```bash
bundle install
bundle exec rails s
```

Open:

```text
http://localhost:4000/
```
